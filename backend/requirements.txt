fastapi>=0.118.0
uvicorn[standard]>=0.34.0
sqlmodel>=0.0.24
pydantic>=2.12.0
python-multipart>=0.0.20
# Turso / libSQL — used when TURSO_DATABASE_URL is set. Falls back to
# a local sqlite file otherwise (built into the Python stdlib).
sqlalchemy-libsql>=0.1.0
