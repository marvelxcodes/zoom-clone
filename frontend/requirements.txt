fastapi>=0.118.0
sqlmodel>=0.0.24
pydantic>=2.12.0
python-multipart>=0.0.20
# Turso / libSQL — used when TURSO_DATABASE_URL is set. Local dev falls
# back to plain sqlite3 which is in the Python stdlib.
sqlalchemy-libsql>=0.1.0
